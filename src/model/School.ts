// Add this interface at the top of your script
interface School {
    id?: string;
    phoneNo: string;
    password: string;
    schoolCode: string;
    moduleName: string;
    username?: string;
    
  }